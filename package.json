{
  "name": "task-service",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend-express",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w backend-express && npm run build -w frontend",
    "dev:backend": "npm run dev:watch -w backend-express",
    "dev:frontend": "npm run dev -w frontend",
    "test": "npm run test -w backend-express",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  },
  "lint-staged": {
    "{backend-express,backend}/**/*.ts": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
